# iox-cpp vcpkg registry

This branch is the immutable Git registry for published `iox-cpp` vcpkg versions.

Registry versions are created by the trusted publication workflow after successful native CI. Published version trees are never rewritten; `versions/baseline.json` advances to the newest published version.
